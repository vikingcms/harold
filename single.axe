@include partials.header.axe

    <div class="mx-auto mb-32 max-w-4xl">
        <div class="p-10">
            <h1 class="text-5xl font-black mb-2">{{ post.title }}</h1>
            <p class="text-gray-800 text-sm uppercase tracking-widest font-medium mb-10">Created {{ post.created_at }}</p>
            <div class="post">
                {{ post.body }}
            </div>
        </div>
        
    </div>

@include partials.footer.axe